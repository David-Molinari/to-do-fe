import './App.css';
import Auth from './components/Auth';
import ToDo from './components/ToDo';

function App() {

  // check for token to see if logged in
  // if so, send to page; if not, send to auth

  return (
    <div className="App">
      
    </div>
  );
}

export default App;
